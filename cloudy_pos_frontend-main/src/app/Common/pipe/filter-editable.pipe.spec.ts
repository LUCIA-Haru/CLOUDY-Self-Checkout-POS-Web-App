import { FilterEditablePipe } from './filter-editable.pipe';

describe('FilterEditablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEditablePipe();
    expect(pipe).toBeTruthy();
  });
});
